include svg.pug

mixin halfSection(title, image)
  .half-section
    .half-section__image-title-container
      h2.half-section__title=title
      .half-section__image
        +svg(image)
    .half-section__content
      block
